# ==========================================
# Strapi 5 Environment Variables Template
# ==========================================
# IMPORTANT: Copy this file to .env and replace all values with your own
# NEVER commit .env to version control!

# ------------------------------------------
# Server Configuration
# ------------------------------------------
HOST=0.0.0.0
PORT=1337

# ------------------------------------------
# Environment (development, production, staging)
# ------------------------------------------
NODE_ENV=development

# ------------------------------------------
# Security Keys
# ------------------------------------------
# CRITICAL: Generate strong, unique secrets for production!
# To generate secure keys, run:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

# App Keys (comma-separated, at least 2 keys recommended)
APP_KEYS="generate-key-1,generate-key-2"

# API Token Salt (for API token generation)
API_TOKEN_SALT=generate-strong-salt-here

# Admin JWT Secret (for admin authentication)
ADMIN_JWT_SECRET=generate-strong-secret-here

# Transfer Token Salt (for data transfer operations)
TRANSFER_TOKEN_SALT=generate-strong-salt-here

# JWT Secret (for user authentication)
JWT_SECRET=generate-strong-secret-here

# Encryption Key (for encrypting sensitive data - REQUIRED in Strapi 5)
ENCRYPTION_KEY=generate-strong-encryption-key-here

# ------------------------------------------
# Database Configuration
# ------------------------------------------

# SQLite (Default for development)
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# PostgreSQL (Uncomment and configure for production)
# DATABASE_CLIENT=postgres
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=strapi
# DATABASE_USERNAME=strapi
# DATABASE_PASSWORD=strapi
# DATABASE_SSL=false
# DATABASE_SCHEMA=public
# DATABASE_URL=postgresql://username:password@localhost:5432/strapi

# MySQL (Uncomment and configure if using MySQL)
# DATABASE_CLIENT=mysql
# DATABASE_HOST=localhost
# DATABASE_PORT=3306
# DATABASE_NAME=strapi
# DATABASE_USERNAME=strapi
# DATABASE_PASSWORD=strapi
# DATABASE_SSL=false

# Database Connection Pool (Optional)
# DATABASE_POOL_MIN=2
# DATABASE_POOL_MAX=10
# DATABASE_CONNECTION_TIMEOUT=60000

# ------------------------------------------
# Optional: Cloud Services
# ------------------------------------------

# AWS S3 (for media uploads)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# AWS_BUCKET=your-bucket-name

# Cloudinary (alternative for media uploads)
# CLOUDINARY_NAME=your-cloud-name
# CLOUDINARY_KEY=your-api-key
# CLOUDINARY_SECRET=your-api-secret

# ------------------------------------------
# Optional: Email Service (for notifications)
# ------------------------------------------
# EMAIL_PROVIDER=sendgrid
# EMAIL_PROVIDER_API_KEY=your-sendgrid-api-key
# EMAIL_DEFAULT_FROM=noreply@yourdomain.com
# EMAIL_DEFAULT_REPLY_TO=support@yourdomain.com

# ------------------------------------------
# Optional: Analytics & Monitoring
# ------------------------------------------
# SENTRY_DSN=your-sentry-dsn
# GOOGLE_ANALYTICS_ID=UA-XXXXXXXXX-X

# ------------------------------------------
# Optional: Feature Flags
# ------------------------------------------
# FLAG_NPS=true
# FLAG_PROMOTE_EE=true
